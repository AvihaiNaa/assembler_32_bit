#ifndef _SECOND_PASS_H
#define _SECOND_PASS_H
/* Processes a code line in first pass */
#include "global.h"


/**
 * Processes a single line in the second pass
 * @param environment - env data structure
 * @return Whether succeeded.
 */
status process_single_line_by_second_pass(env**);


#endif